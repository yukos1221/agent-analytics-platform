# pnpm configuration
# Allows packages to access dependencies from root node_modules
# This enables shared ESLint and TypeScript across the monorepo

# Hoist dependencies (enables finding root-level eslint)
shamefully-hoist=true

# Use shared lockfile
shared-workspace-lockfile=true
